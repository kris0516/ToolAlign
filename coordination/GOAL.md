# S0 持续目标与执行约束

2026-09-06 用户修正后的执行依据。长期 goal 继续 ACTIVE；本文件补充原目标，不重建目标，也不把既有进度清零。

S0 持续监督 ToolAlign 的 P00–P09：按依赖完成契约、硬件与后端校准、数据治理、本地工具与 oracle、SFT、偏好/DPO、固定协议评测、受限本地 serving、独立复核和实际级别交接。只有真实证据满足对应阶段门才登记完成；负结果、人工待审和 NOT_RUN 均须保留。具体范围以任务包、MASTER_PLAN 和正式 ADR 为准。

## 模型设置与消息方向

- **S0：gpt-6-astra，「最高」推理等级，保留用户已经设好的当前值；不得调用工具改写或降低。**
- **独立 worker/reviewer 子任务：gpt-6-astra，thinking=xhigh（极高）。** 仅使用 App 独立任务与隔离 worktree，禁止 sub-agent。
- 发给 S0 的消息只传接收方身份与正文，完全省略 model/thinking。设置参数作用于接收方，不能因为发送者是 xhigh 就把该值传给 S0。S0 发给已确认子任务时才可显式指定子任务设置。
- 所有恢复提示词、自动跟进与新分发都遵循此区分；旧目标中的派发模型指子任务。用户已恢复 S0 设置，无需再设置一次或猜测「最高」的内部枚举。

## 推进与验收

P00 已经独立审查、冻结、合并并完成 main 验证；继续现有 P01/T1、P02/D1，不重复领取或重做 P00。最多两个活跃实现任务，独立 CPU 审查可并行；全局只有一个持租约的重 GPU 作业。共享依赖/契约由 S0 处理，经独立审查、CI、合并和集成验证后发布新基线。

各实现任务先自查并提交精确 SHA，R1 审核该 SHA，S0 才能合并。学习与数据语义抽查须由 kris 真实完成；需要决定时先准备具体可检查材料，不能用模型自评替代。维护 AGENTS 项目简介、完成与部署记录、看板、ADR 和私有任务映射。

继续既定费用、隐私、资源与公开边界：不擅自付费、开放公网推理、上传模型/原始数据/私有资料或接受额外条款。每 30 分钟自动跟进仅在有意义的进展、完成、失败或需要用户动作时通知；目标未完成不得为结束当前回复而标记 complete。

原生 goal 工具在当前会话只支持查询与完成/阻塞状态，没有修改活动目标正文的接口。因此新约束以本文件、AGENTS、协作协议和已更新的自动跟进保存；不声称原目标正文已被工具重写。
