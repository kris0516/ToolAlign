# S0 持续目标与执行约束

2026-09-06 用户修正后的执行依据。完整长期目标保留；本文件补充原目标，不重建目标，也不把既有进度清零。实际运行状态以App及本机映射为准。

S0 持续监督 ToolAlign 的 P00–P09：按依赖完成契约、硬件与后端校准、数据治理、本地工具与 oracle、SFT、偏好/DPO、固定协议评测、受限本地 serving、独立复核和实际级别交接。只有真实证据满足对应阶段门才登记完成；负结果、人工待审和 NOT_RUN 均须保留。具体范围以任务包、MASTER_PLAN 和正式 ADR 为准。

## 统一模型与消息设置

- **S0 和所有独立 worker/reviewer：model=gpt-6-astra，thinking=max（App 中文「最高」）。** 用户已将正在进行的原生 goal 正文改为 max，S0 已通过 get_goal 读回确认，状态继续 ACTIVE、无新增预算。
- 本机 App 中文资源明确 max=最高。新建或授权调整任务时使用这个确切枚举；此前子任务 xhigh/极高规则全部废止，不得恢复或降低。
- S0、T1、D1、R1 已分别通过原生消息工具提交 gpt-6-astra/max 设置。普通回报给 S0 时省略 model/thinking，以保留已设好的 max；任务恢复/新分发使用最新规则。
- 不改写旧实验或派发事实；历史 xhigh 记录仅说明过去实际配置，不构成后续设置授权。仅使用 App 独立任务与隔离 worktree，禁止 sub-agent。

## 推进与验收

P00 已经独立审查、冻结、合并并完成 main 验证；继续现有 P01/T1、P02/D1，不重复领取或重做 P00。最多两个活跃实现任务，独立 CPU 审查可并行；全局只有一个持租约的重 GPU 作业。共享依赖/契约由 S0 处理，经独立审查、CI、合并和集成验证后发布新基线。

各实现任务先自查并提交精确 SHA，R1 审核该 SHA，S0 才能合并。学习与数据语义审查按用户明确指定的参与方式记录，不能把模型审查写成人类本人完成。2026-09-07 kris已将本批两套P02材料委托AI审阅，按ADR-0022接收独立填写副本及整改发现，无须本人重复抄填；其他人工参与规则保持。维护 AGENTS 项目简介、完成与部署记录、看板、ADR 和私有任务映射。

继续既定费用、隐私、资源与公开边界：不擅自付费、开放公网推理、上传模型/原始数据/私有资料或接受额外条款。每 30 分钟自动跟进仅在有意义的进展、完成、失败或需要用户动作时通知；目标未完成不得为结束当前回复而标记 complete。

目标更新来源：用户已直接修改正在进行的活动目标正文，S0 已读回 max；本文件、AGENTS、协作协议、分发模板与自动跟进跟随该实际目标。原 P00–P09 范围、历史进度和已验收证据保持有效。
