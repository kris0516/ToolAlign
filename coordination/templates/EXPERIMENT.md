# <RUN-ID>｜实验记录

状态：PLANNED / RUNNING / COMPLETED / FAILED / ABORTED

研究问题：　预注册比较：　主要指标：

Git commit / dirty flag：　硬件/系统：　backend 与 lock hash：

模型 revision / adapter / quantization：　tokenizer / template / thinking mode：

数据 manifest / split：　seed：　上下文/批次/累积：　训练 token 口径：

DPO reference（若适用）：　preference/oracle 版本：

生成预算 / decode config / cache 模式：

## 命令与时间

实际命令（去除密钥）、开始/结束、暖机、计算、验证、保存、重试；未运行就不得填写实测结果。

## 结果

成功数/总数/比例、类别分布、失败、置信区间口径、MLX peak/RSS/系统压力分别记录。

## 局限与结论

是否支持原假设、哪些混杂因素、单训练 seed 的限制、需要重跑什么。

## 产物

原始日志索引、checkpoint/hash、metrics、抽样 traces、是否允许公开、审查人。
