# P00｜修订二交接

owner：S0；初始 base：`0f152e287bbc0e1c3edfb3f6f3794eb8d36c422f`；首轮被审 head：`15706079c9516197b67dd59a19a0d0c4aa5adea8`；修复实现 head：`6c71c9cc8c5ce5c8fc9ffee06437545f482c7071`；branch：`work/p00-bootstrap-contracts`；日期：2026-09-06。

此交接在后续文档提交附加。独立复核应以分发消息里的完整新 candidate SHA 为准，不把本文件包含的实现 head 当作自动通过。

## 交付与证据

R1 首轮 FAIL 的 2 个 P1 和 3 个 P2 均由 S0 修复。具体范围、命令、退出码、日志 SHA-256 和限制见 `reports/P00_REVISION_2.md`。58 项基础测试、46 项原始 R1 probes、ruff、契约摘要、index/工作副本公开扫描和 wheel 独立安装检查全部通过；本次执行者为 S0，仍需独立复核。

修改集中在 validator、v1 pattern、公开扫描器及必要负例。wire 字段、六个模块接口、GPU 锁策略和依赖锁未扩展。ADR-0010 保留变更理由，contracts.v1.lock.json 更新真实字节；首轮 R1 报告及失败证据完整保留，未改动其断言。

## 请求独立复核

核对首轮 `1570607` 到新候选的修复 diff，并按 G0 抽查完整 base..head。逐项复现 R1-01–05 的关闭，重跑原始 probes、基础检查和新 wheel；新增负例限修复影响面，避免扩展为 P01–P03。

复核对话只能提交 `coordination/handoffs/P00-review-r2.md` 与 `reports/review/P00-r2/`。不得修改实现、原测试期待值、契约、依赖、BOARD/STATUS/DECISIONS；只给精确 SHA 的 PASS/FAIL/BLOCKED。S0 负责处理问题和最终 main 集成。

## 尚未完成

P00 的独立复核、main 合并及集成验证；P01–P03 未派发。模型/数据下载、训练/评测/服务均 NOT_RUN，无费用/公网服务/公开模型数据上传。后续首批为 P01/T1 与 P02/D1，但依赖门未解锁。
