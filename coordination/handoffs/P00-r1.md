# P00｜交接 r1

owner：S0；base：`0f152e287bbc0e1c3edfb3f6f3794eb8d36c422f`；实现 head：`6022b49cf5ab7d2b619c9398c61251fe257ba3ea`；branch：`work/p00-bootstrap-contracts`；日期：2026-09-06。

本交接在后续独立文档提交中附加，R1 必须核对分发消息里给出的完整候选 head，不能只靠缩写或把本自测当独立 PASS。

## 完成范围

本地 clone 并正式领取 S0；可安装 CPU Python 包、五类 wire schema 与语义校验、六个模块 Protocol、受限输入投影、GPU lease、CPU tests/CI、包构建、公开扫描、冻结摘要、AGENTS 项目简介/部署记录及持久恢复入口。

未实现训练、数据管线、工具执行、oracle、模型 serving；P01–P03 尚未派发。无模型下载/加载/训练，无付费 API 或云资源，无模型/数据上传。

## 文件与契约

- 公共接口：`src/toolalign/contracts/`；`configs/protocol.v1.json`；`contracts.v1.lock.json`。
- 资源：`src/toolalign/runtime/`；common Git dir 的 flock，私有 owner 记录。
- 包/CLI/依赖/CI：`pyproject.toml`、`uv.lock`、`src/toolalign/cli.py`、`.github/workflows/cpu.yml`。
- 验证：`tests/contracts/`、`tests/runtime/`、原创 fixtures 和两项检查脚本。
- 说明与治理：`docs/12_CONTRACTS_V1.md`、AGENTS、README、coordination 与 ADR-0007–0009。

版本为 toolalign.contracts.v1 候选，wire schema 为 example/tool/preference/run/trace.v1。S0 是公共依赖/契约/正式状态的唯一修改方。

## 验证证据

见 `reports/P00_SELF_CHECK.md`：34 项 CPU tests、ruff、契约摘要、公开扫描、sdist/wheel 构建、独立临时环境 wheel 安装及 CLI 验证均退出 0。MLX/PyTorch 在 wheel 验证环境中未安装。原始 stdout/stderr 与 SHA-256 索引留本机私有 evidence，R1 可独立读取。

GitHub Public 与 push/admin 权限已通过 connector 读回；Git push dry-run 已成功。远端 CI 与 main 集成待实际 push/审查/合并后验证，不在此预称通过。

## 关键讲解

模型输入与监督/评测标签分开，是为了避免泄漏 oracle；JSON 合法、调用合法、执行完成、任务成功是四个不同判据。Preference 标签必须来自独立语义 oracle，schema 只能验证声明的形状。GPU worktree 隔离不等于硬件隔离，所以必须共用 Git common dir 下的 OS 文件锁；删除锁文件可能导致多个 inode 被分别持锁，因此这里只释放 flock。

## 风险与下一步

P0：尚无自测已知阻断项，但待 R1 独立查验。P1：Python/MLX/backend 兼容仍待 P01；protocol token/deadline 是未实测配置候选。P2：公开扫描只是启发式，GitHub CI 待读回，跨仓库 GPU 作业仍靠 S0 统一调度。

希望 R1 重点验证：schema/Protocol 是否足够且无自相矛盾；危险 schema 是否离线拒绝；supervised target 是否进入模型输入；偏好 split/tie/unknown 门；资源锁跨进程/worktree、异常/超时是否失效；wheel 包含 JSON schema；公共扫描和冻结清单是否有效。

R1 只在独立 worktree 写 `coordination/handoffs/P00-review-r1.md` 和必要的审查复现文件，不修改实现/期待值/看板。输出 PASS/FAIL/BLOCKED、精确被审 base/head、必要复现和 P0/P1/P2。禁止合并 main。S0 修复后再请求同一 R1 复核，P00 合并验证前不发 P01–P03。
